# PSUSphere Docker
This project contains a Django application container with Docker.
Allows to running app consistently accroos different machines.

## Prerequisites
Before running the project, make sure you have:
- Docker installed ([Docker Desktop](https://www.docker.com/products/docker-desktop))
- Docker Compose installed (if using `docker-compose.yml`)

Check installation:
```bash
docker --version
docker-compose --version